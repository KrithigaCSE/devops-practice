pipeline {
    agent any

    stages {
        stage('Clone') {
            steps {
                echo 'Cloning Project'
                sh 'pwd'
                sh 'ls'
            }
        }

        stage('Build') {
            steps {
                echo 'Build Completed Successfully'
            }
        }
    }
}
