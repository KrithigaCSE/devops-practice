pipeline {
    agent any

    stages {
        stage('Clone') {
            steps {
                echo 'Cloning Project'
            }
        }

        stage('Build') {
            steps {
                echo 'Build Completed Successfully'
            }
        }
    }
}
